# @nx/angular declares optional peers (ng-packagr, @angular-devkit/build-angular)
# whose ranges make npm's strict peer resolver backtrack into an Angular 21/22
# conflict, even though neither package is installed or used here.
# Remove once nx/@nx/angular 23.1 stable ships with corrected peer ranges.
legacy-peer-deps=true
